---
layout: base
title: Generative Grid
---

<article class="generative-grid">
  {% for i in range(4) -%}
  <div style="
  --aspect-ratio: {{ ["1", "3/2", "2/1"] | random }};
  --col-start: {{ range(6) | random }};
  --col-span: {{ range(1, 5) | random }};
  --color: {{ ["tomato", "green", "hotpink"] | random }}
  ">{{ i }}</div>
  {% endfor %}
</article>